apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Amprion-S-HE-Chart.fullname" . }}-test-connection"
  labels:
    {{- include "Amprion-S-HE-Chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Amprion-S-HE-Chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
